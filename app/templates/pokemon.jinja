{% extends 'home.jinja' %}
{% block content %}
<h2>Pokemon</h2>
    <form action="" method="post">
         {{  post_form.hidden_tag() }}
        <p>
            {{ post_form.postblock.label }}
            {{ post_form.postblock(size=32) }}
        </p>
        <p>{{ post_form.submit(class='btn btn-primary') }}</p>

    </form>


{% endblock %}
